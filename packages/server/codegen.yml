overwrite: true
schema: http://localhost:4000/graphql
generates:
  src/types.d.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
