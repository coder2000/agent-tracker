type AuthResponse {
  token: String
  user: User!
}

input AuthInput {
  accessToken: String!
}

type Query {
  me: User!
}

type Mutation {
  authenticate(input: AuthInput!): AuthResponse!
}

type User {
  firstName: String
  surname: String
  emailAddress: String!
  googleToken: String
}
