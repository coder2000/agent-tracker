mutation authenticate($input: AuthInput!) {
  authenticate(input: $input) {
    token
  }
}

query getMe {
  me {
    firstName
    surname
    emailAddress
  }
}

query getIsLoggedIn {
  isLoggedIn @client
}
